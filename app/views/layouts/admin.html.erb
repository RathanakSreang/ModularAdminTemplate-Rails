<!DOCTYPE html>
<html>
  <head>
    <title>ModularAdmin Template</title>
    <meta http-equiv="content-type" content="text/html; charset=utf-8">
    <meta charset="utf-8" />
    <meta http-equiv="Content-Language" content="en"/>
    <meta name="description" content="" />
    <link rel='canonical' href='<%= request.url %>'/>
    <link rel='alternate' href='<%= request.url %>' hreflang='x-default'/>

    <%= yield(:meta_for_seo) %>
    <%= yield(:meta_facebook) %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tag %>
    <%= render "layouts/shim" %>
    <%= favicon_link_tag "icon-logo.ico" %>
    <%= stylesheet_link_tag    'admin', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="<%= Rails.env if Rails.env.development? %>">
    <div class="main-wrapper">
      <div class="app" id="app">
        <%= render "layouts/admin/header" %>
        <%= render "layouts/admin/sidebar" %>
        <article class="content <%= params[:action] %>-page">
          <%= yield %>
        </article>
        <%= render "layouts/admin/footer" %>
        <%#= render "common/modal_center" %>
        <pre>
          <%= debug(params) if Rails.env.development? %>
        </pre>
      </div>
    </div>
    <div class="ref" id="ref">
      <div class="color-primary"></div>
      <div class="chart">
        <div class="color-primary"></div>
        <div class="color-secondary"></div>
      </div>
    </div>
  </body>
  <%= javascript_include_tag 'admin', 'data-turbolinks-track': 'reload' %>
</html>
